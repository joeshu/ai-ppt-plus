cases:
  - {id: conflict, signal: same_fact_different_values, expected: stop_and_request_authority}
  - {id: visual_text, signal: visual_25_outline_18, expected: use_outline_18_and_log}
  - {id: no_renderer, signal: missing_render_tool, expected: nonzero_and_blocked}
  - {id: missing_art, signal: key_visual_unavailable, expected: documented_placeholder}
  - {id: repair_cap, signal: round_3_still_critical, expected: manual_required}
  - {id: stale_handoff, signal: hash_changed_without_revision, expected: revision-required}
  - {id: stale_render_report, signal: deck_hash_differs_from_render_or_inspection_report, expected: block_delivery_and_rerender}
  - {id: state_drift, signal: handoff_state_differs_from_manifest_state, expected: block_delivery_and_reconcile_state}
  - {id: false_runtime, signal: runtime_env_vars_present_but_node_or_artifact_tool_missing, expected: interface_only_or_blocked}
  - {id: orphan_asset, signal: slide_references_asset_missing_from_external_manifest, expected: blocker}
  - {id: fake_visual_intermediate, signal: native_ppt_shapes_or_pptx_render_claimed_as_visual_intermediate, expected: return_to_visual-draft_and_invoke_image_generation}
  - {id: generic_visual, signal: generated_image_fails_requested_premium_soe_style_gate, expected: regenerate_before_reconstruction}
  - {id: no_image_tool, signal: image_generation_unavailable_after_bounded_retry, expected: report_unavailable_and_block_or_request_explicit_waiver}
  - {id: reconstruction_redesign, signal: reference_layout_changed_without_user_approval, expected: restore_reference_geometry_and_log_tradeoff}
  - {id: flattened_reference, signal: whole_slide_bitmap_contains_required_text_and_simple_shapes, expected: fail_editability_gate_and_rebuild_objects}
  - {id: fake_complex_asset, signal: missing_key_illustration_replaced_with_low_quality_imitation, expected: remove_imitation_and_create_documented_placeholder}
  - {id: visual_dimension_mismatch, signal: rendered_and_reference_pixels_have_different_dimensions, expected: align_render_dpi_or_block_comparison}
  - {id: visual_threshold, signal: approved_reference_comparison_below_calibrated_threshold, expected: revision-required}
  - {id: ocr_unavailable_strict, signal: requested_ocr_language_model_missing_with_require_ocr, expected: blocker_with_unavailable_status}
  - {id: ocr_unavailable_optional, signal: requested_ocr_language_model_missing_without_require_ocr, expected: pass_with_quality_degradation_and_human_review}
  - {id: signoff_missing_field, signal: structured_closeout_missing_or_false_decision, expected: human-closeout}
  - {id: quality_report_not_consumed, signal: child_quality_report_failed_but_project_or_delivery_report_omits_it, expected: blocker_and_quality_evidence}
  - {id: single_reference_multipage, signal: one_reference_image_given_for_deck_with_multiple_slides, expected: block_and_request_reference_directory}
  - {id: missing_reference_page, signal: approved_reference_directory_missing_a_slide, expected: blocker_with_slide_number}
  - {id: route_conflict, signal: route_says_visual_creation_but_visual_authority_is_approved_reference, expected: route_validation_blocker}
  - {id: visual_creation_without_evidence, signal: visual_creation_route_missing_generated_manifest, expected: route_validation_blocker}
  - {id: reconstruction_without_roster, signal: reference_reconstruction_route_missing_reference_roster, expected: route_validation_blocker}
  - {id: editability_untyped, signal: strict_manifest_has_no_per_object_L0_L5_records, expected: editability_validation_blocker}
  - {id: l0_flattened_full_slide, signal: required_text_or_simple_shapes_inside_full_slide_bitmap, expected: delivery_blocker}
  - {id: l4_required_placeholder, signal: required_complex_asset_is_only_a_placeholder, expected: delivery_blocker}
  - {id: l3_without_acceptance, signal: traceable_static_graphic_has_no_reduced_editability_acceptance, expected: delivery_blocker}
  - {id: l2_missing_provenance, signal: independent_image_has_no_source_or_provenance, expected: editability_validation_blocker}
  - {id: editability_summary_mismatch, signal: declared_page_level_counts_differ_from_objects, expected: manifest_validation_blocker}
  - {id: report_missing, signal: required_child_report_missing_from_project_index, expected: project_aggregate_blocker}
  - {id: report_stale, signal: indexed_report_or_deck_hash_changed_after_aggregation, expected: project_aggregate_blocker}
  - {id: report_hidden_failure, signal: child_report_failed_but_project_aggregate_marks_passed, expected: project_aggregate_blocker}
  - {id: icon_swallowed_by_frame, signal: icon_roster_item_missing_from_frame_exclusion_or_icon_layer, expected: blocker_and_reextract}
  - {id: icon_duplicate_layers, signal: same_icon_present_in_frame_and_independent_icon_layer, expected: blocker_and_remove_duplicate}
  - {id: icon_edge_touch, signal: cutout_icon_touches_or_is_truncated_at_asset_edge_without_acceptance, expected: blocker_and_recrop_or_regenerate}
  - {id: icon_alpha_failure, signal: residual_background_or_empty_alpha_after_cutout, expected: blocker_and_recutout}
  - {id: decorative_word_art_as_text, signal: artistic_typography_treated_as_formal_text_without_asset_record, expected: classify_as_decorative_art_L2_or_placeholder}
  - {id: icon_b4_roster_missing, signal: icon_bearing_reference_has_no_source_vs_frame_review_or_roster, expected: blocker_and_reextract}
  - {id: icon_b5_evidence_missing, signal: icon_manifest_lacks_cutout_method_contact_sheet_or_frame_preview, expected: blocker_and_run_b4_b5_chain}
  - {id: icon_unassigned_layer, signal: visible_asset_is_in_neither_frame_asset_ids_nor_icon_asset_ids, expected: blocker_and_reconcile_layers}
  - {id: imagegen_manifest_missing, signal: imagegen_asset_layer_has_no_per_page_provenance_manifest, expected: blocker_before_composition}
  - {id: imagegen_provenance_incomplete, signal: imagegen_manifest_missing_generated_source_prompt_file_or_non_imagegen_backend, expected: blocker_and_regenerate_asset}
  - {id: icon_direct_asset_bypass, signal: icon_or_decoration_copied_directly_without_imagegen_asset_record, expected: blocker_and_run_imagegen_b4_b5}
  - {id: icon_partial_imagegen, signal: reference_reconstruction_imagegen_manifest_covers_only_problem_icons, expected: blocker_and_regenerate_all_icon_layers}
