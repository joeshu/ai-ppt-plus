name: ai-ppt-plus CI

on:
  push:
  pull_request:

jobs:
  contract-and-regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install deterministic rendering dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libreoffice poppler-utils inkscape
          python -m pip install --upgrade pip
          python -m pip install numpy pillow python-pptx pyyaml openpyxl cairosvg fonttools
      - name: Verify SVG rendering dependency
        run: inkscape --version
      - name: Compile Python sources
        run: python -m py_compile scripts/*.py tests/test_*.py
      - name: Validate package and routing contracts
        run: |
          python scripts/validate_skill_package.py --skill-dir .
          python scripts/validate_routing_contract.py
      - name: Run schema, DAG, package and golden regressions
        run: python scripts/run_tests.py --report test-report.json
      - name: Run strict P2 guard smoke tests
        run: python tests/test_p2_guards.py
      - name: Check whitespace
        run: git diff --check
      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ai-ppt-plus-test-report
          path: test-report.json
